<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty name="SERVICE_NAME" source="spring.application.name" defaultValue="collections-service"/>
    <springProperty name="APP_VERSION" source="app.version" defaultValue="1.0.0"/>
    <springProperty name="ENVIRONMENT" source="environment" defaultValue="local"/>

    <!-- Console appender with JSON encoding for structured logs -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>
                {
                    "service": "${SERVICE_NAME}",
                    "version": "${APP_VERSION}",
                    "environment": "${ENVIRONMENT}"
                }
            </customFields>
            <includeMdc>true</includeMdc>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <maxDepthPerThrowable>30</maxDepthPerThrowable>
                <maxLength>2048</maxLength>
                <shortenedClassNameLength>20</shortenedClassNameLength>
            </throwableConverter>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Service-specific loggers -->
    <logger name="com.cardbilling.collections" level="DEBUG"/>
    <logger name="org.springframework.web" level="WARN"/>
    <logger name="org.springframework.security" level="WARN"/>
    <logger name="io.resilience4j" level="INFO"/>
</configuration>
